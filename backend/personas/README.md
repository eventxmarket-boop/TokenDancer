# Published Personas

This directory will contain the normalized, publish-ready persona packs that the frontend and backend use in production.

V1 rule:

- Every persona must follow the same file structure.
- Published personas should be promoted from `backend/seed_personas/`.
