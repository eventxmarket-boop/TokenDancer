# Self Persona Schema

This directory defines the first-stage entry for creating a self persona.

The product uses the `self-skill` split:

- `work_system.md` describes how the person makes decisions and gets work done.
- `reply_persona.md` describes how the person speaks, responds, and keeps boundaries.

The first release only needs the structure and explanation layer. It does not implement a full distillation pipeline yet.
